def playfaircipher():
    
    print "Would you like to: "
    encrypt = input("1")
    decrypt = input("2")
    if encrypt = 1:


    

def option():
    key=input("Enter key: ")
    key=key.replace(" ", "")
    key=key.lower()



def matrix(x,y,initial):
    
    k=0    
    mymatrix=matrix(5,5,0)
        for i in range(0,5):
            for j in range(0,5):
            mymatrix[i][j]=result[k]
            k+=1
# create 5 x 5 table 
table = [['e', 'x', 'c', 'l', 's'],
         ['i', 'o', 'r', '_', 'a'],
         ['b', 'd', 'f', 'g', 'h'],
         ['j', 'k', 'm', 'n', 'p'],
         ['t', 'v', 'w', 'y', 'z']]
    from string import ascii_lowercase 
    alphabet = ascii_lowercase + ' '
    character = ' '
    for character in key:
        if character not in result:
            if character == 'u':
                result.append('v')
            if character == 'q':
                result.append('k')
            else:
                result.append(character)
def encrypt():
    message = input('Enter a message to encrypt: ')
# breaking up message into two character chunks
    message = ' '
    n  = 2 
    [line[i:i+n] for i in range(0, len(line), n)]
# function for characters in same column

# function for characters in same row 
    #for i in range(len(character 
# function for characters that are in neither same row or column 

#def decrypt():


#playfaircipher()




